import React from 'react';
import { PreLoader } from '../../../assets/icon';

import './Loader.scss';

const Loader = () => {
	return (
		<div className="Loader">
			<PreLoader />
		</div>
	);
};

export default Loader;
